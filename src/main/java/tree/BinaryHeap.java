package tree;

public class BinaryHeap {
	/*
	1. It is complete Tree. For leaf level keys are at left. Suitable for storing them in array
	2. Min Heap/ Max Heap. Parent key is min to its children recursively for min heap. reverse is Max heap.
	 
	 */
}
